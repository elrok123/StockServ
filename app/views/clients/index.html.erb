<h1> List of Clients </h1>

<table>
  <tr>
    <th>Name</th>
    <th>Email</th>
  </tr>
 
  <% @clients.each do |client| %>
    <tr>
      <td><%= client.first_name %> <%= client.last_name %></td>
      <td><%= client.email %></td>
    </tr>
  <% end %>
</table>
